/*
 * CardEvent Messages
 *
 * This contains all the text for the CardEvent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CardEvent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CardEvent component!',
  },
});
